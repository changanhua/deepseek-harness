name: Owner-only guard example

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  guard-example:
    if: github.actor == 'changanhua'
    runs-on: ubuntu-latest
    steps:
      - name: Explain policy
        run: echo "Owner-only workflow guard is active."
