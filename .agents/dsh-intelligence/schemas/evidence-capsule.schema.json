{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "dsh-intelligence/schemas/evidence-capsule.schema.json",
  "title": "Evidence Capsule",
  "description": "回答当前 checkout/profile/环境到底是什么；静态事实与运行观察严格分层。来自 docs/dsh-post-training-system-design.md §4。",
  "type": "object",
  "required": ["schema_version", "id", "target_snapshot"],
  "properties": {
    "schema_version": { "type": "integer", "const": 1 },
    "id": { "type": "string", "pattern": "^evidence-" },
    "created_at": { "type": "string", "format": "date-time" },
    "target_snapshot": {
      "type": "object",
      "required": ["repository", "revision", "branch"],
      "properties": {
        "repository": { "type": "string" },
        "revision": { "type": "string" },
        "branch": { "type": "string" },
        "upstream_base": { "type": "string" },
        "dirty_paths": { "type": "array", "items": { "type": "string" } },
        "profile": { "type": "string" },
        "host_scope": { "type": "string" }
      }
    },
    "static_manifest": {
      "type": "object",
      "properties": {
        "dump_config_ref": { "type": "string" },
        "generated_catalogs": {
          "type": "array",
          "items": {
            "type": "object",
            "properties": {
              "kind": { "type": "string" },
              "digest": { "type": "string" },
              "source_ref": { "type": "string" }
            }
          }
        },
        "exact_source_refs": { "type": "array", "items": { "type": "string" } }
      }
    },
    "runtime_observations": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "collector": { "type": "string" },
          "observed_at": { "type": "string", "format": "date-time" },
          "scope": { "type": "object" },
          "claim": { "type": "string" },
          "artifact_ref": { "type": "string" }
        },
        "required": ["collector", "observed_at", "claim"]
      }
    },
    "unavailable": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "fact": { "type": "string" },
          "reason": {
            "type": "string",
            "enum": ["not-mounted", "no-live-instance", "unsupported", "not-requested"]
          }
        }
      }
    },
    "conflicts": { "type": "array" }
  }
}
