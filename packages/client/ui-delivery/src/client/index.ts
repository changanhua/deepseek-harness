/** Empty Personal Delivery browser scaffold. */

/** The scaffold requires no client service before a concrete workbench exists. */
export const inject: string[] = []

/** Load successfully without registering slots, locale, Remote calls, or UI. */
export function apply(): void {}
