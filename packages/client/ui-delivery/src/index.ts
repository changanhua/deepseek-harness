/**
 * Personal Delivery UI plugin, node half. The browser half is discovered from
 * the package manifest and `./client` export.
 */

/** Host plugin body; this surface owns no host behavior. */
export function apply(): void {}
