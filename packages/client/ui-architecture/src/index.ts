/** Architecture workspace node half; the browser half is discovered from `dsh.client`. */

/** No Host behavior: this entry only makes the browser bundle discoverable. */
export function apply(): void {}
