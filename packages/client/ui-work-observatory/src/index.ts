/**
 * Work Observatory browser activity producer, node half. The browser half
 * installs the document-scoped tracker through the package's client export.
 */

/** Host plugin body — no host-side behavior for this browser-only package. */
export function apply(): void {}
